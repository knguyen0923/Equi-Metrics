fastapi==0.141.1
uvicorn[standard]==0.52.0
pymongo==4.17.0
pydantic==2.13.4
pydantic-settings==2.14.2
email-validator==2.3.0
bcrypt==5.0.0
pyjwt==2.13.0
httpx==0.28.1
slowapi==0.1.10
python-multipart==0.0.32
pandas==3.0.5
xgboost==3.3.0
numpy==2.5.1
# xgb.XGBRanker() imports sklearn internally even though this app never
# calls into it directly — omitting it crashes app.ml.registry's import
# (and so the whole app, since app.main imports it eagerly) with
# "ImportError: sklearn needs to be installed in order to use this module".
scikit-learn==1.9.0

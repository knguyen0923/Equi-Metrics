[pytest]
; Puts backend/ itself on sys.path so tests can do both `from app...`
; and `from tests.fakes import ...` regardless of the directory pytest
; is invoked from.
pythonpath = .
testpaths = tests

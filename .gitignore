# Python
__pycache__/
*.pyc
.venv/
venv/

# Env files
.env
.env.local

# Editor / OS
.DS_Store

# Local Claude Code tooling config — machine-specific, not part of the app
.claude/

# Raw ML training exports (large, not meant for git — see
# backend/scripts/build_ml_data.py, which trims these into
# backend/app/ml/data/ which IS committed)
ml-models/*.csv
ml-models/*.pkl
